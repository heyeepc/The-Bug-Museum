【Bug 记录】Python input() 导致的类型错误
错误标签： #数据类型错误 #输入处理 #Python基础

错误现象： 无法对输入的数据进行 sum() 数学计算，或者循环遍历数字时数字被拆散（如 12 变成了 '1' 和 '2'）。

根本原因： Python 的 input() 函数接收到的所有内容，一律是字符串（String）！ 哪怕用户输入的是 100，Python 拿到的也是 "100"。

核心教训（口诀）： 只要用 input，必做 type 转换！ 想做数学计算，必须先用 int() 或 float() 包裹起来。
